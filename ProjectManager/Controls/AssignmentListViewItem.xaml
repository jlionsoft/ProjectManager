<UserControl x:Class="ProjectManager.Controls.AssignmentListViewItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjectManager.Controls"
             xmlns:c="clr-namespace:ProjectManager.Converter"
             mc:Ignorable="d">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Stretch" Margin="0 5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border Grid.Column="1">
            <Border Width="10" Height="10" CornerRadius="5"
                VerticalAlignment="Center" Margin="7"
                    Name="border_priority"/>
        </Border>
        <StackPanel Grid.Column="0" HorizontalAlignment="Left">
            <TextBlock Name="tb_title" FontWeight="Bold"/>
            <Label Name="lbl_duedate"/>
        </StackPanel>
    </Grid>
</UserControl>